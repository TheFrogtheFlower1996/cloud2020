server:
  port: 80 #端口号

spring:
  application:
    name: cloud-consumer-order #服务名
  cloud: #consul注册中心
    consul:
      host: localhost
      port: 8500
      discovery: #服务发现名称
        service-name: ${spring.application.name}








