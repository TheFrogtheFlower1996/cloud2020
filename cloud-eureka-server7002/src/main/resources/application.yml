server:
  port: 7002

eureka:
  instance:
    hostname: eureka7002.com #eureka服务端实例名
  client:
    register-with-eureka: false #表示不向eureka注册自己
    fetch-registry: false #表示自己就是注册中心，我的职责是维护服务实例，并不需要去检索实例
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
